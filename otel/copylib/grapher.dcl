*

 declaratives.
  file-error section.
      use after standard error procedure on
      dokumer grapher tahmin txtimp.
  file-error-status.
      if fs-txtimp = "35"
         open output txtimp
         close txtimp
         open i-o txtimp
      else
         perform fileerr-proc
      end-if
      .
  file-exit.
  end declaratives.

*
